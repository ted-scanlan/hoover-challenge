class Hoover {
  constructor(input) {
    this.room = input[0].split(" ").map(Number)
    this.coords = input[1].split(" ").map(Number)
    this.dirtCoords,

    this.patchesCleaned = 0
  }
}



module.exports = {
       Hoover
     };
